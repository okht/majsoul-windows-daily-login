{
  "name": "majsoul-windows-daily-login",
  "version": "0.1.0",
  "private": true,
  "description": "Passively open Mahjong Soul on Windows on a daily schedule, confirm the lobby, then exit.",
  "keywords": ["majsoul", "windows", "edge", "task-scheduler", "local-only"],
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check:no-input": "node scripts/check-no-input.mjs",
    "check:privacy": "node scripts/check-privacy.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm test && npm run check:no-input && npm run check:privacy",
    "acceptance": "node src/cli/acceptance.mjs"
  },
  "dependencies": {
    "@napi-rs/keyring": "1.3.0",
    "nodemailer": "9.0.3",
    "playwright-core": "1.61.1",
    "proper-lockfile": "4.1.2",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "acorn": "8.17.0",
    "acorn-walk": "8.3.5",
    "vitest": "4.1.10"
  }
}
